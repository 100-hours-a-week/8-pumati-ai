def build_badge_prompt(number: int, theme: str = "circuit", style: str = "digital") -> str:
    return f"A round cute simple gold badge with number {number} in the center, {theme}-themed background, {style} style, high resolution"
